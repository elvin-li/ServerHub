---
name: Homarr
desc: Good-looking service start page (can run alongside ServerHub)
vars:
  - name: HOST_PORT
    label: Web port
    default: "7575"
category: dashboard
featured: true
tags: [dashboard]
ports: ["7575"]
url_template: "http://{{HOST_IP}}:{{HOST_PORT}}"
---
services:
  homarr:
    image: ghcr.io/ajnart/homarr:latest
    container_name: homarr
    restart: unless-stopped
    ports:
      - "{{HOST_PORT}}:7575"
    volumes:
      - ./configs:/app/data/configs
      - ./icons:/app/public/icons
      - ./data:/data
